<!DOCTYPE html>
<html>
<head>
	<title>Yearbook It!</title>	
	<%= stylesheet_link_tag "scaffold" %>
	<%= stylesheet_link_tag "topicos" , :media => "all" %>
	<%= javascript_include_tag :defaults %>
	<%= csrf_meta_tag %>
</head>

<body id="store">
	<div class="cmpLogar">
		<p class="logar">
        <% if user_signed_in? %>
          Logado como <strong><%= current_user.email %></strong>.
          <%= link_to 'Configurações da Conta', edit_user_registration_path, :class => 'navbar-link' %> |
          <%= link_to "Sair", destroy_user_session_path, method: :delete, :class => 'navbar-link'  %>
        <% else %>
          <%= link_to "Registrar", new_user_registration_path, :class => 'navbar-link'  %> |
          <%= link_to "Logar", new_user_session_path, :class => 'navbar-link'  %>
        <% end %>
        </p>
	</div>

	<div id="banner">
		<%= image_tag("logo2.gif") %>
		<%= @page_title || "" %>
	</div>

	<div id="menu">
		<a id="cmpMenu" href="http://localhost:3000">Home</a>
		<a id="cmpMenu" href="http://localhost:3000/usuarios">Usuários</a>		
	</div>

	<div id="columns">
		<div id="main">
			<%= yield %>
		</div>
	</div>
</body>

</html>
